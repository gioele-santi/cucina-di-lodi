---
layout: default
title: Home
---

{% comment %} Generate localized date {% endcomment %}
{% assign months = "Gennaio|Febbraio|Marzo|Aprile|Maggio|Giugno|Luglio|Agosto|Settembre|Ottobre|Novembre|Dicembre" | split: "|" %}

<div class="posts">
  {% for post in paginator.posts %}
  <div class="post">
    <h1 class="post-title">
        <!-- <a href="{{ site.baseurl }}/{{ post.url }}"> -->
      <a href="{{ post.url }}">
        {{ post.title }}
      </a>
    </h1>

        {% assign m = post.date | date: "%-m" | minus: 1 %}
        {% assign day = post.date | date: "%d" %}
        {% assign month = months[m] %}
        {% assign year = post.date | date: "%Y" %}
      
    <p><span class="post-date post-description">Pubblicato il {{ day }} {{ month }} {{ year }}</span>
    
        {% if post.categories[0] != null %}
            <span class="post-description"> Categorie: 
            {% for category in post.categories %}
                <a class="category-button" href="">{{ category }}</a>
            {% endfor %}
            </span> <br/>
        {% endif %}

        {% if post.tags[0] != null %}
            <span class="post-description">Tag: 
            {% for tag in post.tags %}
                <a class="tag-button" href="">{{ tag }}</a>
            {% endfor %}
            </span>
        {% endif %}
        
    </p>
    
    
    {% if post.images[0] != null %}
        {% assign image = post.images[0] %}
        <div class="img_crop">
            <img src="{{ image.url }}" alt="{{ image.alt }}">
        </div>
        
    {% else %}
        <!-- Immagine assente -->
    {% endif %}
      
    {{ post.excerpt }}  
    <a class="continue-button" href="{{ post.url }}">Continua a leggere</a>
  </div>
  {% endfor %}
</div>

<div class="pagination">
  {% if paginator.next_page %}
    <a class="pagination-item older" href="{{paginator.next_page_path}}">Precedenti</a>
  {% else %}
    <span class="pagination-item older">Precedenti</span>
  {% endif %}
  {% if paginator.previous_page %}
    {% if paginator.page == 2 %}
      <a class="pagination-item newer" href="{{ site.baseurl }}/">Recenti</a>
    {% else %}
      <a class="pagination-item newer" href="{{paginator.previous_page_path}}">Recenti</a>
    {% endif %}
  {% else %}
    <span class="pagination-item newer">Recenti</span>
  {% endif %}
</div>

